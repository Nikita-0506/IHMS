# Enterprise Test Architecture

This directory supports Phase 10 quality assurance segmentation.

- `authentication/` login, token, and permission tests
- `api/` REST contract and endpoint behavior tests
- `ai_models/` AI inference and confidence validation tests
- `database/` persistence and integrity tests
- `security/` authz and threat protection tests
- `integration/` cross-module end-to-end tests
